# Markdown — strategic notes stay local; README is the one public doc
*.md
!README.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# uv / venv
.venv/
venv/
env/
.python-version

# Build / dist
build/
dist/
*.whl

# Triton / CUDA artifacts
.triton/
*.cubin
*.ptx
*.fatbin

# Notebooks
.ipynb_checkpoints/

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Project-local working dirs (one-off probes + raw run artifacts stay local)
scratch/
results/
*.log
wandb/

# fleet (GPU VM state + pulled artifacts)
.fleet.json
out/

# Datasets (PTB-XL etc. — fetched on demand, never committed)
data/
*.h5
*.zarr
*.npy
*.pkl

# Secrets / credentials
.env
.env.*
!.env.example
credentials.json
*.pem

# Box-burst artifacts (deploy tars, pulled output dirs, kernel IO bundles)
/*.tgz
*_out/
box_out_*/
e2_config_seed/
*_bundle_*.pt

# Media + local pulls (demo renders and pulled box output never ship)
video/
*.mp4
*.mov
*.gif
box_pull/
results_pull_*/
deliverables/
